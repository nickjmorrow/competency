import { InstructorsState } from '~/instructors/types/InstructorsState';

export const instructorsInitialState: InstructorsState = {
    instructors: [
        {
            instructorId: 1,
            name: 'Katya Zamo',
        },
    ],
};
